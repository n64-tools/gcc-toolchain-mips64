# jobs:       
#   Compile-Toolchain-Windows:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v2

#       - name: Install required dependencies
#         run: |
            # Definitely required
            sudo apt-get install -yq libmpfr-dev libmpc-dev texinfo

            # Maybe required
            sudo apt-get install -yq wget bzip2 gcc g++ make file zlib1g-dev git gcc-multilib
            
            # Required when it was Azure...
            # sudo apt-get install -yq build-essential software-properties-common
            # sudo apt-get install -yq gcc-10
            sudo apt-get install -yq g++-10 mingw-w64 libgmp-dev bison
            sudo apt-get install -yq byacc
            # sudo apt-get install -yq zip gzip tar
            
            # might be new requirements...
            sudo apt-get install -yq gcc-9-locales wine wine64

#       # Build the toolchain if toolchain files changed w.r.t default which is
#       - name: Build x86_64 Toolchain
#         run: |


#       - name: Upload Toolchain Artifacts
#         uses: actions/upload-artifact@v3
#         with:
#           name: "windows-x86_64-toolchain"
#           path: ${{ github.workspace }}/**/win64/**/*
